import { StyleSheet } from "react-native";

const styles=StyleSheet.create({
    container:{
        flexDirection:'colum',
        backgroundColor:"white",

    },
    inline:{
        flexDirection:'row',
    }
})