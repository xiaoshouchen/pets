import React, {Component} from 'react';
import {StackNavigator} from 'react-navigation';
import {StyleSheet, Text, View, ScrollView, Button, Image} from 'react-native';

class ArticleTypeModal extends React.Modal {
    render() {
        return (
           <Text>这是一个模态框</Text>
        );
    };
}
